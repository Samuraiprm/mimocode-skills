# Быстрый старт - MiMoCode Skills

## Установка

### Вариант 1: Клонировать целиком (рекомендуется)

```bash
git clone https://github.com/Samuraiprm/mimocode-skills.git ~/.mimocode/skills/
```

### Вариант 2: Копировать отдельные навыки

```bash
# Frontend-дизайн навыки
cp -r taste-engineering ~/.mimocode/skills/
cp -r design-engineering ~/.mimocode/skills/
cp -r impeccable ~/.mimocode/skills/
cp -r review-animations ~/.mimocode/skills/
cp -r animation-vocabulary ~/.mimocode/skills/
cp -r premium-visual-design ~/.mimocode/skills/
cp -r minimalist-ui ~/.mimocode/skills/
cp -r industrial-brutalist-ui ~/.mimocode/skills/
cp -r redesign-existing ~/.mimocode/skills/
cp -r full-output-enforcement ~/.mimocode/skills/

# Безопасность
cp -r code-audit ~/.mimocode/skills/
cp -r code-review ~/.mimocode/skills/
cp -r pentest ~/.mimocode/skills/

# IoT навыки
cp -r iot-* ~/.mimocode/skills/
```

### Вариант 3: Клонировать в папку проекта

```bash
cd ваш-проект
git clone https://github.com/Samuraiprm/mimocode-skills.git .mimocode/
```

## Первые шаги

### Frontend-дизайн

```
Создай landing page с использованием taste-engineering
```

```
Проведи ревью анимаций в этом компоненте используя review-animations
```

```
Проведи аудит существующего проекта используя redesign-existing
```

### Безопасность

```
Проведи code review этого PR
```

```
Проведи аудит безопасности этого endpoint
```

### IoT

```
Проведи анализ прошивки этого устройства используя iot-apktool
```

```
Сканирую сеть на наличие IoT устройств используя iot-nmap
```

## Список всех навыков

Смотри [README.md](README.md) для полного каталога.
